name: artifact-evaluation
run-name: ${{ inputs.task }} / ${{ inputs.arguments }}
on:
  workflow_dispatch:
    inputs:
      task:
        type: choice
        required: true
        options:
          - task_1
          - task_2
      arguments:
        type: string
        required: true
jobs:
  task_1:
    if: ${{ inputs.task == 'task_1' }}
    name: task_1 / ${{ inputs.arguments }}
    runs-on: ubuntu-latest
    env:
      ARGUMENTS: ${{ inputs.arguments }}
    steps:
      - uses: actions/checkout@v6
      - name: Run task_1
        run: bash task_1.sh "$ARGUMENTS"
  task_2:
    if: ${{ inputs.task == 'task_2' }}
    name: task_2 / ${{ inputs.arguments }}
    runs-on: ubuntu-latest
    env:
      ARGUMENTS: ${{ inputs.arguments }}
    steps:
      - uses: actions/checkout@v6
      - name: Run task_2
        run: bash task_2.sh "$ARGUMENTS"
